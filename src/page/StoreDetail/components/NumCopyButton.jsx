import React, { useState } from 'react';

const NumCopyButton = () => {

    return (
        <div>
            <button className="cursor-pointer flex gap-1 text-xs leading-4 font-medium text-[#059669]">
                복사
            </button>
        </div>
    );
};

export default NumCopyButton;